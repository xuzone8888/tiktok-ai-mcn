# ==========================================
# 阿里云服务器环境变量配置
# ==========================================
# 复制此文件为 .env.local 并填入实际值
# 使用方法: cp deploy/env.local.template .env.local

# ------------------------------------------
# 1. Supabase 配置 (必需)
# ------------------------------------------
# 从 Vercel 环境变量中获取这些值
# 或从 Supabase Dashboard > Settings > API 获取
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ------------------------------------------
# 2. 豆包 (Doubao) API 配置 (必需)
# ------------------------------------------
# 从 Vercel 环境变量中获取
# 或从火山引擎 ARK 平台获取
DOUBAO_API_KEY=your-doubao-api-key
DOUBAO_ENDPOINT_ID=your-endpoint-id

# ------------------------------------------
# 3. Sora2 视频生成 API 配置 (必需)
# ------------------------------------------
# 从 Vercel 环境变量中获取
# 或从数创科技平台获取
SORA2_API_KEY=your-sora2-api-key
SUCHUANG_API_KEY=your-suchuang-api-key

# ------------------------------------------
# 4. 应用配置
# ------------------------------------------
# 使用服务器 IP 或域名
# 示例: http://123.456.789.012:3000
# 或: https://cn.tokfactoryai.com
NEXT_PUBLIC_APP_URL=http://your-server-ip:3000

# ------------------------------------------
# 5. 管理员配置
# ------------------------------------------
# 管理员邮箱 (用于后台访问权限控制)
ADMIN_EMAIL=admin@example.com

# ------------------------------------------
# 6. 可选配置
# ------------------------------------------
# 如果需要自定义 API 端点
# SUCHUANG_API_ENDPOINT=https://api.wuyinkeji.com
# DOUBAO_API_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3/chat/completions






