# 独立交付版本验证 — 2026-09-17

分支：`codex/tiktok-private-preview-release-20260917`。基线：`4e3033ad`。

## 隔离与验证

- 独立 Git 工作树中验证，未覆盖原开发工作区或当前本地测试服务。
- 本轮预览及任务展示依赖单独纳入；未纳入原工作树的账号页、多平台界面、sidebar、loading 和多任务发布页改动。
- 全量 Node 测试：493 项，492 通过、1 跳过、0 失败。与此前整个开发工作树的 501 项不同，是因为本分支不纳入无关的跨平台/整页语言重构测试；原有已提交测试仍全部运行。
- TypeScript type-check、目标 ESLint、diff whitespace 检查通过。
- Node 20 production build 通过，使用测试库配置，仅构建、不启动候选服务；包含两个 `/api/publish/previews` route。
- 构建有 Supabase 模块的 Edge Runtime API 警告，没有把它描述为零警告；真实远程存储验证尚未执行。
- 没有复制任何 `.env.local` 到交付工作树，配置只在构建进程中加载；不提交依赖、构建产物、密钥、日志或视频。

## 基线历史和部署限制

本分支保留此前三个本地 release/安全修复前置提交：`168a79b8`、`f64844a6`、`4e3033ad`。它们不是本次新增预览修改，但属于当前代码基线。不要把分支与最新生产分支的所有差异都归因于本轮，也不要未经整合审核直接合并部署。

迁移 `20260916_tiktok_private_previews.sql` 已在测试库执行，禁止盲目重放。没有生产迁移、OSS 配置、真实媒体上传、清理运行、TikTok 发布或生产部署。本次推送只用于同事核对实现和准备测试基础设施；代码发布不代表基础设施已就绪。

待同事回传独立测试 Bucket、地域和安全检查结果，安全配置凭证后，在独立本地候选实例完成 readiness、上传、持久读取、越权拒绝和删除清理验收。
