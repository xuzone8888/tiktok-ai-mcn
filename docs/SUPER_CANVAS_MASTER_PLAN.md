# 超级画布总纲(SUPER CANVAS MASTER PLAN)

> **本文件是「超级画布」项目的执行总纲与新窗口恢复入口。**恢复工作:①读本文件;②读 docs/EXECUTION_TRACKER.md 末尾「当前方向」;③按 §九 分期从当前期继续,一边做一边验证。
> 定稿于 2026-07-12:LibTV 实地走查(用户登录态,零积分消耗)+ 4 路网络研究 + 4 视角对抗复审(易用性/稳定性/范围/积分)后综合。用户裁决记录见 §一。

## 一、目标与裁决(用户原话为准)

- **形态**:超级画布(对标 liblib.tv LibTV 画布),自由无限画布 + 卡片节点 + 连线,能真出片。
- **验证目标**:工业级视频生成,满足**大型漫剧**与**电商视频工作室**使用(两者都是目标,不许砍;节奏=电商先行、漫剧随 P2 跟上)。
- **要求**:易上手易使用;**每个功能交付后使用流程稳定、正常**;LibTV 不相关内容裁掉;积分先搭框架(数值占位/参照 LibTV),后续整体优化;长程任务,一边做一边验证。
- **愿景**:画布是公有的——任何人的画布可分享进来、也可嵌出去,生态融合(→ §八 分享分级)。
- **历史包袱**:v1「全屏导演工作台」方案(artifact 732bdf3c)被画布方向取代;其导演层引擎设计(三器官串联/两段式/幂等重渲)折入脚本节点(§六)。2026-07-11 被否决的 17-agent 方案要点(两抽屉合并/片段卡小画布/2×2 卡片墙)仍不许复活。

## 二、范围:两条黄金旅程定边界(不用「80%」)

范围唯一事实源 = 本节 + §三 三列表。**判据:凡不在两条旅程关键路径上的 LibTV 功能,默认裁。**

- **旅程 A·电商(P1 可走通,≤7 步)**:上传商品图/贴链接 → 商品节点(卖点解析)→ 图片节点(场景图/多角度)→ 视频节点(图生视频)→ 合成 → 直发 TikTok / 下载。
- **旅程 B·漫剧(P2 可走通,≤9 步)**:一句话/剧本 → 脚本节点(分镜表)→ 资产装配(角色/场景,九宫格+多角度)→ 批量生分镜图 → 批量生视频 → 合成 → 下载。
- 每期发布前用真人各走一遍双旅程:记录步数与卡点(停留>60s 无操作=卡点),卡点>2 不发布。

## 三、对标裁决三列表(LibTV 功能 → 做/裁/延)

**【做】**(期次见 §九):无限画布+双击/Tab建节点/拖入/侧边+拉线/成组/整理画布/小地图/网格吸附/Ctrl+Z;文本/图片/视频/商品/脚本/合成 6 类节点(**v1 白名单,新增节点须对标复核+用户裁决**);生成器挂节点下方(@引用素材/参数胶囊/积分预估/费用汇总条);脚本节点分镜表(镜号/时长/描述/景别/光影/对白/音效/运镜,自由文本+预设 chips);资产自动识别挂槽(从描述认出角色);角色资产卡(全身+面部+表情九宫格+多角度板,**漫剧依赖,P2**);宫格切分(P2,服务漫剧资产);图片高清/裁剪/整图重生成;视频模式按**能力矩阵**渲染(模型支持啥显啥,不照抄五 tab);历史资产库(直读现有表);内容节点删除二次确认;组件级错误边界+「尝试恢复/重新加载画布」;空态快捷位;分享=只读链接+复制到我的空间(带依赖清单)。

**【裁】**(LibTV 不相关内容,用户指示删掉):3D 导演台(用多角度图生成平替)/悠船·MJ 系模型/风格广场社区市场/特效库/Agent 会话 API/25宫格·剧情推演·全景·打光等长尾 `/` 功能/**指令式局部重绘+标记 chip(绑定同裁,半残功能不留)**/**音频独立节点(无时间线消费方=死胡同;TTS 作脚本/视频节点参数)**/QQ·微信登录/会员营销弹窗。

**【延】(触发条件才做)**:风格模板市场(P4 社区发布起量后)/实时多人协作 Yjs(P4,团队客户明确要)/成片时间线(P4 评估,**必须以合成节点数据结构为底座演进**)/hold_settle 预扣计费(P3,大额批量场景+悬挂释放对账脚本齐备才立项)/拆解节点(P4,复用 blueprint-drawer isDeconstruct 分支,structure_only 纪律原样继承)。

**变更控制**:范围只做等量交换不做净增;每期结束复盘才允许调整一次,记录「加了什么、换出去什么」。

## 四、架构决策(ADR)

1. **底盘 = React Flow(MIT)+ 自研媒体节点 + zustand**。tldraw($6k/年+水印)备选;ComfyUI 前端 GPL 禁抄;自研兜底 6-9 人月不走。媒体策略:节点默认 poster 缩略图,**选中才挂 `<video>`**(DOM 断言进 DoD),同屏活跃视频 ≤6,zoom 低于阈值节点降级色块(语义缩放)。
2. **执行留在现有链路,画布只是编排视图**。铁律:**双轨期画布零 fork 任何 API/执行器/store 原语,出现"必须 fork"即停手上会。**明确:BTM 是**浏览器内**编排器(不是服务端),画布沿用之;**节点执行状态唯一真相源 = 服务端 generations 表**,画布文档只存 `nodeId→taskId/generationId` 引用,BTM 写回仅作乐观 UI。
3. **单写者**:同一画布任一时刻仅一个写者标签(navigator.locks,P0 交付不许推迟),其余标签只读+横幅。
4. **统一对账合约(P1 前置交付物)**:每类节点注册 `{jobKey, fetchStatus(jobKey), pollInterval, terminalTimeout}`;触发点=画布加载/visibilitychange 回前台/手动刷新;超时判死必触发幂等退款。验收:提交后 kill 标签→重开→状态收敛到服务端真值。
5. **画布文档 schema**:`canvases` 表,jsonb 存**拓扑+引用**(nodes/edges/groups + schemaVersion + deps 依赖清单[模型/音色/角色/配方 ID,v1 即有字段可空]);**禁存 dataURL/签名 URL,只存 OSS object key**(渲染层换签名 URL+内存缓存);文档 >512KB 拒存并提示;保存=节点级补丁(op log),非重叠自动 rebase;本地影子副本用 IndexedDB(不占 localStorage 5MB 配额),重载 shadow>server 提示一键恢复。
6. **坏档防御**:加载走 zod 校验,非法节点降级为「损坏节点」占位卡(可删),**永不整画布白屏**;迁移注册表(v1→v2→…),「上一 schema 版本文档能打开」进 DoD。LibTV 实测崩过 React #185——错误边界+恢复按钮照抄,并加 store 层防护(见 §七 性能预算)。
7. **合成/批量类重任务一律 DB 排队**(local-stitch 并发闸=2、3.4G 小机),节点显示排队位,禁止 fail-fast 报错甩用户。
8. **路由**:新 `/canvas`(P0 起),不动 `/studio`。**omnibox 长期保留为轻量快捷入口,画布只收编多节点编排型链路;幻灯片/图文帖明确不进画布 v1**。三库同源(积分/角色/历史),画布「从历史选择」直读现有 generations/assets/blueprints,不建平行表。
9. **收编量化判据**(全满足才改导航,再观察 14 天才可折叠旧入口):画布路径提交→成片完成率 ≥ /studio 同链路;P95 出片时长不劣化;连续 14 天零 P0/P1 缺陷;积分对账抽样 100 单零差异。

## 五、节点规格 v1(6 类白名单)

每个节点统一具备:输入(上游连线/上传/历史选择)、生成器面板(模型+参数胶囊+@引用+积分预估+发送)、状态机(idle→queued→running→done/failed;failed 单独重试;刷新经对账恢复)、产物动作(下载/入库/推为参考/直发)。

| 节点 | 说明 | 后端复用 |
|---|---|---|
| 文本 | 文案/剧本/提示词载体,可被 @ 引用 | 无新增 |
| 图片 | 文生图/垫图;工具条=高清/裁剪/整图重生成/**多角度(P2)/宫格切分(P2)** | /api/generate/image + gpt-image-2 |
| 视频 | 能力矩阵驱动模式(文生/图生/首尾帧…按所选模型渲染);特效/运镜进参数 | models/submit 7 模型网关 |
| 商品 | 商品图/链接→卖点卡(P1 轻量版=上传图+文本;链接解析 P3) | product-vision / parse-reference |
| 脚本 | **= blueprints 的画布宿主**,见 §六 | blueprints + 蓝图抽屉组件级复用 |
| 合成 | 多视频→拼接成片(DB 排队);(后期)轮播模式承接幻灯片能力评估 | ai-gen/stitch 双通道 |

灰置控件必须点击可得「如何解锁」指引(如视频节点无上游图:「需要一张图片,[创建图片节点]/[从历史选择]」);所有模式/参数名配一句话人话+悬停示例。

## 六、脚本节点(漫剧心脏,P2)

- **不重写蓝图编辑器**:双击节点打开现有 blueprint-drawer(组件级复用,不 fork);2026-07-05 刚过人工走查的能力(台词行内编辑/hook 矩阵/配方实例化/卖点勾选)全部继承。
- **唯一增量**:①分镜表视图(镜号/时长/描述/景别/光影/对白/音效/运镜列,自由文本+预设 chips,描述支持 @资产);②资产装配步:从描述自动识别角色挂「待创建槽」,三来源(AI 生成/画布选/上传)+「全部自动生成」默认按钮;③节点顶栏动作:重新生成/批量生分镜图/批量生视频/导出。
- **导演引擎**(v1 方案折入):deepseek 撰稿(时长感知口语化)+ segmenter 气口分段 + qwen hooks 四型开场;**两段式**:机械稿即时呈现(零白屏)→导演稿异步 diff 覆写,每镜可还原;导演稿免费(入 usage 流水+滥用闸:每用户 50 次/日、单蓝图免费重生成 5 次,超出按脚本生成价)。
- **门控软化**(对电商用户):「资产不齐」默认走自动生成/可跳过,只警示「哪些镜会降质」;硬门控仅零输入。九列由引擎填满,用户只改不填。
- **批量体验**:批次进度面板(总进度+逐镜四态格+积分实扣/实退实时数)、失败镜一键单独重试(成功镜不重扣)、节点角标 n/m 常驻。

## 七、易用性与稳定性硬条款

**易用性(逐条绑期次,见 §九)**
- 空态四快捷位=点击生成**已连好线+示例内容+首输入框聚焦**的完整节点链:①图生视频(最短爽点)②商品图→带货视频 ③一句话→分镜脚本 ④文案→配音视频;未上线功能的快捷位不渲染。
- 费用汇总条(发送钮上方恒显:明细+总额+余额)替代弹窗;拦截式确认仅限「余额<预估×1.2 或 单次>5000⚡」。
- undo 只覆盖画布文档;删除 running 节点三选一确认(取消并退款/仅移除[任务继续,产物进历史]/返回);网关不支持取消则明示。
- 最小支持 1366×768:侧栏默认图标态、小地图默认收起、生成器面板超视口自动 dock 底部、胶囊≥5 折叠「更多」;此分辨率走查进每期 DoD。
- 常驻「?」入口=快捷键表+两个交互式教程(电商版/漫剧版,跟手走完最短链);完成奖励小额积分(=积分发放链路首个真实用例)。

**性能预算(P0 DoD 数值验收)**:200 节点画布 pan/zoom ≥50fps(中端机);冷加载 <3s;100 节点+30 视频 poster 内存 <800MB;jsonb >2MB 告警建议拆画布;错误边界触发进监控(单画布 >1 次/日报警)。

**每期「稳定使用流程」验收模板(操作→预期,尽量脚本化,`.temp/canvas-p{n}-verify.mjs` 惯例)**
- P0:空态快捷位可点/建节点编辑 5s 后刷新零丢失/断网 30s 恢复自动补存/双标签第二个只读+横幅/塞坏 node json 画布照开+占位卡/100 节点保存<1s/Ctrl+Z。
- P1:预估=实扣逐条比对/提交后 kill 标签重开经对账恢复/注入上游失败→failed+恰好一条 refund/狂点发送只一个 job 一次扣/双标签无双跑双扣/超阈值确认。
- P2:20 镜批量、人为败 2 镜→仅失败镜可重试不重扣/向导中途刷新回原步资产不丢/门控灰钮给出缺项。
- P3:合成 10 并发排队消化零静默丢单/分享链接免登录只读可看/复制遇「模型下线/音色无权限」逐项红字+可替换,不复制出坏画布/链接解析失败可重试零扣费。
- 通用:tsc+build+实弹脚本+当日积分对账差异=0+对抗审查 workflow(大改动)。

## 八、积分框架(P1 搭好,数值零变更)

**铁律:框架期不新增、不修改任何价目;新节点接入沿用其宿主链路现价**(图片 1K/2K/4K=5/10/15、视频=各模型现价、assembly 1⚡/镜先渲后扣、hook/导演稿免费、拼接=assembly 现价对齐 **不另立 0 价**)。LibTV 参照价仅作后续定价讨论输入(2K图⚡18/Seedance2.0 5s720P⚡135/脚本⚡6;年费四档 499/1299/2999/6599 元)。

- **双模式声明**:每 action 显式 `billing_mode`:`post_charge`(≤5⚡ 内部渲染:拼接/TTS镜,先渲后扣失败零扣费无退款面,与 assembly 既有裁决对齐)/`hold_settle`(**P3 才启用**,大额第三方任务)/`free`(导演稿/hook,入 amount=0 流水+每日配额闸,服务端 429 fail-fast)。
- **P1 交付**:①estimate API(`src/lib/credits/pricing.ts` 单一常量模块导出 `estimateCredits(action,model,params)`,estimate 与结算 import 同一函数;带 `PRICING_VERSION`,提交回传,服务端重算>预估则拒单要求刷新)——**同 PR 收敛散落 4 处的定价**(deduct 路由 CREDIT_PRICING/types VIDEO_MODEL_PRICING/lib/credits.ts@deprecated/actions.generate-video)。②**下线两个无鉴权路由 `/api/user/credits/{deduct,refund}`**(现状:userId 取请求体+admin client 直改余额+deduct 不写流水——安全洞,P1 前必须处理)。③退款幂等下沉 DB:`credit_transactions` 部分唯一索引 `unique(type,(metadata->>'task_id')) where type='refund'`,插入 on conflict do nothing,应用层 check 仅优化。④流水标准化迁移:type 枚举定死(consume/refund/free_usage/grant)+补 task_id/canvas_id/node_id/batch_id/action_id 结构化列+索引,删 insertCreditTransaction 运行时试插循环;用户侧明细页=按 canvas/batch 聚合只读查询。⑤客户端零金额条款:前端只传 action+params+estimateToken,金额与扣费全在服务端任务提交事务里。
- **批量记账(P2 前置)**:批量=1 batch_id + **N 个子任务独立 task_id 独立扣/退**(直扣模式天然支持部分退;禁止单笔 lump deduct);结果条「成功 x/N,已退 y⚡」;流水按镜可查。并发扣费经 Postgres RPC 单语句原子(`UPDATE ... WHERE credits >= amount`),CAS 循环仅留低频路径。
- **hold_settle(P3)**:`credit_actions`/`credit_holds` 表结构 P1 随迁移落库冻结但不启用(防后续迁移);启用前置=悬挂释放(≤10 分钟自动释放)+对账脚本可查任何悬挂单;结算封顶 `settled_amount ≤ estimated_amount`(库级 check),超时长部分平台吸收永不超收;改价只对新预估生效,在途按 hold 锚定 pricing_version 结算。DDL 草案见评审档(planreview-credits)。

## 九、分期(每期独立可发布;P0 不对外)

| 期 | 内容 | 关键验收 |
|---|---|---|
| **P0 画布骨架**(内部) | React Flow 壳/6 类节点空壳/双击+Tab/连线/成组/上传+历史直读现有表/自动保存(补丁+IndexedDB 影子)/单写者锁/zod+schemaVersion+deps 字段/错误边界/快捷键面板/空态(仅渲染已上线快捷位)/1366×768 | P0 模板 7 项+性能预算数值 |
| **P1 生成接入**(首个灰度) | 图片/视频节点真出片(经 BTM+网关零改动)/**商品节点轻量版**(上传图+文本→卖点卡)/**直发 TikTok 产物动作**/费用汇总条/积分框架 5 件(§八)/统一对账合约/首次教程(电商版) | 「上传图→图生视频→下载/直发」全链+P1 模板 6 项+黄金旅程 A ≤7 步 |
| **P2 脚本节点**(漫剧) | 蓝图画布宿主+分镜表+资产装配(角色槽/九宫格/多角度/宫格切分)+导演引擎两段式+批量生图生视频+批次进度面板+教程(漫剧版)+批量记账 | P2 模板 3 项+黄金旅程 B ≤9 步 |
| **P3 编排完善** | 商品链接解析/合成节点 DB 排队/成组存工作流(与配方分工:配方=内容参数模板留 /templates,工作流=结构模板,互不越界)/分享 v1(只读链接+复制+依赖清单校验)/hold_settle 启用评估 | P3 模板 4 项+收编判据开始计数 |
| **P4 生态与收编** | 发布社区/拆解节点/旧入口按判据折叠/Yjs 实时协作评估/时间线评估(以合成节点数据结构为底座) | 收编量化判据全绿 |

## 十、工程纪律(沿用 EXECUTION_TRACKER 惯例)

- worktree 分支开发,中文 commit(结尾 Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>),**不 push**(push main=生产自动部署,须用户裁决);每子任务过 `npx tsc --noEmit`,涉页面过 `npm run build`;大改动跑对抗审查 workflow 修实锤;迁移 SQL 落盘由用户经 dashboard 执行(exec_sql RPC 生产不存在);每完成一个子任务更新 EXECUTION_TRACKER「当前状态」。
- 每个功能交付定义(用户要求)=「使用流程稳定正常」:走完该期验收模板+真人黄金旅程走查,才算完成。
- LibTV 参考资料:会话 scratchpad `recon-*.md`(六路代码侦查)、`research-*.md`(四路网络研究)、`planreview-*.md`(四视角复审);LibTV 实测细节以本文件 §三-§六 为准。
