# StarGaze 超级画布完整功能点清单(开发 Checklist)

> 定稿于 2026-07-12 三路审查+三层对抗复核(完整性×代码实证×交叉一致性)后仲裁;2026-07-12 四轮 LibTV 补实测后增补组能力/快捷键全表/分镜排序等条目;搭配 SUPER_CANVAS_MASTER_PLAN.md(总纲)与 SUPER_CANVAS_DATA_MODEL.md(数据模型)使用;本清单是开发 checklist 唯一事实源,每期开工按期次过滤本清单。
> 列说明:来源(对标=LibTV实测有/独有=StarGaze自有/复审=对抗复审产物);裁决(做/裁/延);期次(P0-P4,裁=—);复用/新建(复用写具体文件,新建标成本 S/M/L);★资产=相对总纲§六的资产装配系统新增面。
> **轨道命名(2026-07-12 用户裁决)**:双轨=『电商带货』/『剧情创作』(内容创作类命名;覆盖漫剧/短剧/小说推文等场景)。本清单中『漫剧』一词均指剧情创作轨的典型场景,非轨道名。
> **机器守卫(勿破坏)**:全部裁决/期次计数(下方两张统计表)与 P0 看板覆盖由 `scripts/canvas-checklist-reconcile.mjs` 强制核对——改动本表后必须跑一次(`node scripts/canvas-checklist-reconcile.mjs`),红了必须修到绿。**期次填写规则**:脚本按单元格中**首个出现的 Pn** 归一为「起始期」,故**复合/跨期期次合法保留、无需改写**(`P1/P2`、`P2 批量/P3 合成`、`P2 评估`、`P3-P4` 等现存写法均正确)。唯一硬约束——**P0 首落行的期次以裸 `P0` 开头**,「持续沿用」等语义写进备注,**勿写「P0 起」之类后缀**,否则破坏 `| 做 | P0 |` 严格 grep(2026-07-12 审计根因:一处「P0 起」曾致严格 grep 少计 1 行,非漏功能)。

---

## A. 画布壳

| 功能点 | 来源 | 裁决 | 期次 | 复用或新建 | 备注 |
|---|---|---|---|---|---|
| 无限画布 pan/zoom(React Flow MIT 底盘 + zustand) | 对标 | 做 | P0 | 新建 M | ADR1;tldraw 备选,ComfyUI 前端 GPL 禁抄 |
| 双击空白建节点 | 对标 | 做 | P0 | 新建 S | |
| Tab 建节点 | 对标 | 做 | P0 | 新建 S | |
| 拖入文件建节点(上传) | 对标 | 做 | P0 | 复用现有 OSS 上传链路 | 只存 object key |
| 侧边栏+拉线建节点 | 对标 | 做 | P0 | 新建 S | |
| 节点连线(edge) | 对标 | 做 | P0 | React Flow 原生 | |
| 成组(Ctrl/Alt+G) | 对标 | 做 | P0 | React Flow group 新建 S | P3 成组可存工作流 |
| 组工具条:▶ 整组执行(一键顺序执行组内工作流) | 对标 | 做 | P3 | 新建 M(依赖节点执行编排) | 四轮新发现;与工作流模板配套:插入模板→改输入→整组执行 |
| 组工具条:批量下载(组产物打包) | 对标 | 做 | P3 | 新建 S | 四轮新发现 |
| 组工具条:转分镜组(组→分镜组,有序序列概念) | 对标 | 延 | P3 | — | 四轮实证概念存在;序列语义待测后随成组存工作流复议 |
| 组工具条避让顶栏(视口顶部锚定时不被站头遮挡) | 复审 | 做 | P3 | 新建 S | 对标教训:LibTV 窄窗口下组工具条按钮被自家站头遮挡 |
| 整理画布自动布局(Alt+Shift+F) | 对标 | 做 | P0 | 新建 S(dagre/elk 布局) | |
| 小地图 | 对标 | 做 | P0 | React Flow MiniMap | 1366×768 下默认收起 |
| 网格吸附 | 对标 | 做 | P0 | React Flow snapGrid | |
| 缩放控件 + Ctrl+0 适应视图 | 对标 | 做 | P0 | React Flow 原生 | |
| 隐藏连线开关 | 对标 | 做 | P0 | React Flow 原生 S | 已裁决收编:React Flow 原生,成本≈0 |
| 快捷键:Ctrl+L 连线 | 对标 | 做 | P0 | 新建 S | |
| 快捷键:Ctrl+D 复制节点带连线 | 对标 | 做 | P0 | 新建 S | |
| 快捷键:Space 拖画布 | 对标 | 做 | P0 | React Flow 原生 | |
| 快捷键:Delete 删除 | 对标 | 做 | P0 | 新建 S | 联动 G 组删除确认 |
| 快捷键:Ctrl+Z 撤销(undo 只覆盖画布文档,不撤生成任务) | 对标+独有 | 做 | P0 | 新建 M(op log 逆操作) | §七硬条款 |
| 快捷键:解组 Ctrl/Alt+Shift+G | 对标 | 做 | P0 | React Flow 原生 S | |
| 快捷键:节点复制(Alt+拖动节点) | 对标 | 做 | P0 | 新建 S | |
| 快捷键:创建副本(Ctrl+Alt+拖动,带连线) | 对标 | 做 | P0 | 新建 S | 与 Ctrl+D 同族 |
| 快捷键:重做 Ctrl+Shift+Z | 对标 | 做 | P0 | 新建 S(op log 正向重放) | |
| 快捷键:缩放 Ctrl+加减号 | 对标 | 做 | P0 | React Flow 原生 S | |
| 快捷键:Ctrl+Alt+G 合并分镜组 | 对标 | 延 | P3 | — | 四轮实测:组工具条有『转分镜组』按钮,分镜组=一等概念(非普通成组);序列语义待测(按钮被 LibTV 自家站头遮挡未走通);随 P3 成组存工作流一并复议 |
| 空态四快捷位=点击生成已连好线+示例内容+首输入框聚焦的完整节点链(①图生视频 P1 ②商品图→带货视频 P1 ③一句话→剧本节点→分镜脚本 P2 ④文案→配音视频 P2·载体=脚本节点配音路径) | 对标+独有 | 做 | P0 | 新建 M | 壳 P0;每位标首渲期次,未上线不渲染(P0 仅建节点引导);双轨重裁见总纲 §七;LibTV 四位被替换见 C 组末行 |
| 自动保存=节点级补丁(op log),非重叠自动 rebase | 对标+复审 | 做 | P0 | 新建 M | 编辑 5s 后刷新零丢失进验收 |
| ★「输入已更新」dirty 角标(上游产物更新后,下游连线节点自动挂提示) | 对标(五轮实弹新录) | 做 | P1 | 新建 S(节点存上游 generationId 快照,比对即知) | 上下游变更传播语义,缺了它「上游重生成→下游不知情」;随 P1 生成接入落 |
| 连线后上游产物自动进下游生成器引用区(缩略图带序号) | 对标(五轮实弹新录) | 做 | P1 | 新建 S(生成器面板读上游 refs) | 五轮实弹:LibTV 新建下游节点(⊕菜单)同样自动连线+带引用;连线即引用是画布核心语义 |
| IndexedDB 影子副本 + shadow>server 一键恢复 | 独有 | 做 | P0 | 新建 M | 不占 localStorage 5MB 配额 |
| 单写者锁(navigator.locks)+ 双标签第二个只读+横幅 | 独有 | 做 | P0 | 新建 M | ADR3,P0 交付不许推迟 |
| canvases 文档 schema(doc jsonb=拓扑+引用;schema_version/deps 独立列) | 独有 | 做 | P0 | 新建 M(新表迁移) | doc 只含 nodes/edges/groups;schema_version 与 deps(模型/音色/角色/配方 ID,v1 即有字段可空)是独立 DB 列,非 doc 内嵌;transport envelope 才组合三者 |
| 禁存 dataURL/签名 URL,只存 OSS object key(渲染层换签名 URL+内存缓存) | 独有 | 做 | P0 | 新建 S | ADR5 红线 |
| 文档 >512KB 软告警建议拆画布 | 独有 | 做 | P0 | 新建 S | P0-Q1 已裁(2026-07-12):512KB=软告警(不拒存,S7 横幅);硬拒在 >2MB(见 G 组) |
| 断网 30s 恢复自动补存 | 独有 | 做 | P0 | 新建 S | P0 验收模板项 |
| 统一对账合约(每类节点注册 {jobKey, fetchStatus, pollInterval, terminalTimeout};触发=加载/回前台/手动刷新) | 独有(复审) | 做 | P1 | 新建 M | P1 前置交付物;超时判死必触发幂等退款 |
| 新路由 /canvas,不动 /studio | 独有 | 做 | P0 | 新建 S | ADR8 |
| omnibox 长期保留轻量快捷入口 | 独有 | 做 | P0 | 复用 src/components/studio-shell/omnibox.tsx(零改动) | 幻灯片/图文帖明确不进画布 v1 |
| 1366×768 最小适配(侧栏默认图标态/生成器超视口 dock 底部/胶囊≥5 折叠) | 独有 | 做 | P0 | 新建 M | 此分辨率走查进每期 DoD |
| 底部工具栏(添加节点/工作流/素材库/角色库/历史记录/快捷键/教程) | 对标 | 做 | P0 | 新建 S | 壳 P0,各入口随所属期点亮 |
| 常驻「?」快捷键面板 | 对标 | 做 | P0 | 新建 S | 面板只列已生效键 |

## B. 节点

| 功能点 | 来源 | 裁决 | 期次 | 复用或新建 | 备注 |
|---|---|---|---|---|---|
| 6 类节点空壳(可创建/连线/占位渲染,不接生成) | 独有 | 做 | P0 | 新建 S | §九 P0 明列;P0 走查用 |
| 节点统一四件套(输入=连线/上传/历史选择;生成器面板;状态机;产物动作) | 复审 | 做 | P1 | 新建 M(节点基类) | §五统一规格 |
| 产物动作:下载/入库/推为参考 | 独有 | 做 | P1 | 复用 generations 入库链路 | 直发 TikTok 见 E 组 |
| 文本节点(提示词/文案载体,可被 @ 引用) | 对标 | 做 | P0 | 无后端新增,前端 S | |
| 剧本节点(结构化故事简报:标题/类型/时长建议/基调/分场,可编辑,作脚本节点上游) | 对标 | 做 | P2 | 新建 S(文本类变体) | 漫剧起点,双轨起点之一;实现为文本节点 variant:'story_brief',不突破 6 类白名单(裁决依据=2026-07-12 双轨裁决+数据模型设计 §一.2);若用户要最强发现性可升第 7 类,升级无损 |
| 商品节点轻量版(上传图+文本→卖点卡) | 独有 | 做 | P1 | 复用 src/lib/studio/product-vision.ts + blueprints.product jsonb | 电商起点,双轨起点之二 |
| 商品链接解析(失败可重试零扣费) | 独有 | 做 | P3 | 复用 src/lib/studio/reference-parser.ts / parse-reference | P3 验收模板项 |
| 图片节点:文生图 | 对标 | 做 | P1 | 复用 /api/generate/image + gpt-image-2 | |
| 图片节点:垫图/图生图 | 对标 | 做 | P1 | 同上 | |
| 图片节点:指令式整图编辑(如「将背景改为雪夜」) | 对标 | 做 | P1 | 同上 | 区别于已裁的「指令式局部重绘+标记」 |
| 图片 chip:+参考 | 对标 | 做 | P1 | 复用 reference-cache.ts | 即 @ 引用参考图 |
| 图片 chip:标记 | 对标 | 裁 | — | — | 与指令式局部重绘绑定同裁,半残功能不留 |
| 图片 chip:风格 | 对标 | 裁 | — | — | 风格广场已裁;风格走提示词文本+全局风格标签(C 组) |
| 图片 chip:聚焦 | 对标 | 延 | P2 评估 | — | 四轮新发现的第 4 chip,语义待实测(疑似聚焦参考图区域),测明后定裁做 |
| 图片参数:画质(低/标准/高) | 对标 | 延 | P2 | 能力矩阵渲染 S | 枚举完整=低/标准/高;仅支持模型显示。**2026-08-09 用户裁决延 P2**(R2-Q4 混合路线):上游 quality 被 env 门控,且与我方按 resolution 计价的口径脱钩——外部条件不具备,非工时问题 |
| 图片参数:清晰度 1K/2K/4K | 对标 | 做 | P1 | 复用现价 5/10/15⚡ | 框架期零价目变更 |
| 图片参数:比例 13 种 | 对标 | 延 | P2 | 能力矩阵渲染 S | 枚举完整=1:1/1:2/2:1/9:16/16:9/3:4/4:3/3:2/2:3/5:4/4:5/21:9/9:21;实显以模型支持为准。**2026-08-09 用户裁决延 P2**(P1-Q2b):上游 `getVideoPlatformImageSize` 的 `sizeMap` 每档只有 6 个 key(auto/1:1/16:9/9:16/4:3/3:4),末行 `sizeMap[r]?.[ratio] || sizeMap[r]?.auto || null` 会让多出来的档位**静默回落 auto 却按全价扣费**;要补必须先给该 map 补像素尺寸并确认 gpt-image-2 接受,而那是 quick-gen / image-factory 共用链路,不属画布单方改动——外部条件不具备,非工时问题。**连带已做**(P1-Q2a):画布侧画幅枚举已收窄为与上游一致的 6 项并抽成唯一真相源 `CANVAS_IMAGE_ASPECT_RATIOS`,消除契约比能力宽所留的静默错画幅口子 |
| 图片参数:生成数量 1/2/4 张 | 对标 | 延 | P2 | 复用现链路 | 枚举完整。**2026-08-09 用户裁决延 P2**(R2-Q4):上游图片请求体无 `n` 字段,画布加控件也无处透传 |
| 图片模型清单(LibTV 14 个对照) | 对标 | 延 | P2 | 复用我方网关模型枚举 | 悠船 V8.1/V7/Niji7 等 MJ 系明裁;其余以我方在售模型为准。**2026-08-09 用户裁决延 P2**(R2-Q4):全产品在售图片模型只有 1 个,且 `src/lib/canvas/generation-intent.ts` 服务端硬钉 `z.literal("gpt-image-2")`——没有第二个模型可列,选择器是空壳 |
| 图片工具条:高清 | 对标 | 延 | P2 | 复用现有高清链路 | §三明列。**2026-08-09 用户裁决延 P2**(P1-Q3):上游 `VideoPlatformImageParams` **根本没有 mode 字段**(`src/lib/video-platform-image-api.ts:33-42`),主路由让 mode 生效的唯一机制是换 prompt(`api/generate/image/route.ts:389`),而那段固定模板**只在用户提示词为空时才套用**(同文件 :69-71 第二行即早退),画布提示词恒非空(intent 强制 ≥2 码点,`generation-intent.ts:204`)—— **把模板搬到画布是 no-op,画布上没有可复用的东西**。要真做出区别只能由画布发明「模板+用户提示词」这套主路由没有的语义,且三条注入路径全撞墙:客户端拼撞 prompt 全等栅栏 409;进 intent 改指纹致恢复重放 409 `ACTION_ID_CONFLICT`;只能服务端 `submitImage` 里拼,但那会造出「花钱买到的真实输入」不进 spec/不进 `generations.prompt`/不进 `request_fingerprint`,与 `generation-service.ts:930` 写死的不变量冲突(触铁律 2)。**外部条件不具备,非工时问题。**
| 图片工具条:裁剪 | 对标 | 做 | P1 | 新建 S(前端裁剪) | §三明列 |
| 图片工具条:整图重生成 | 对标 | 做 | P1 | 复用生图链路 | §三明列 |
| 图片产物:节点内下载/全屏预览 | 对标 | 做 | P1 | 新建 S | 四轮实测:LibTV 工具条右侧常驻下载+全屏 |
| 商品多角度(nine_grid 模式透传) | 对标 | 延 | P2 | — | 电商刚需,旅程 A 提质。⚠️ **原备注「复用 /api/generate/image 现成 nine_grid 分支(route.ts VALID_IMAGE_MODES 含 nine_grid)S;纯 UI 露出」经实证是错的**——R2-Q4 曾判「工作量估计错了」,实情比那更严重:**那个可复用物不存在**(理由同 #81)。**2026-08-09 用户裁决延 P2**(P1-Q3):上游 `VideoPlatformImageParams` **根本没有 mode 字段**(`src/lib/video-platform-image-api.ts:33-42`),主路由让 mode 生效的唯一机制是换 prompt(`api/generate/image/route.ts:389`),而那段固定模板**只在用户提示词为空时才套用**(同文件 :69-71 第二行即早退),画布提示词恒非空(intent 强制 ≥2 码点,`generation-intent.ts:204`)—— **把模板搬到画布是 no-op,画布上没有可复用的东西**。要真做出区别只能由画布发明「模板+用户提示词」这套主路由没有的语义,且三条注入路径全撞墙:客户端拼撞 prompt 全等栅栏 409;进 intent 改指纹致恢复重放 409 `ACTION_ID_CONFLICT`;只能服务端 `submitImage` 里拼,但那会造出「花钱买到的真实输入」不进 spec/不进 `generations.prompt`/不进 `request_fingerprint`,与 `generation-service.ts:930` 写死的不变量冲突(触铁律 2)。**外部条件不具备,非工时问题。**
| ★资产 图片工具条:多角度 | 对标 | 做 | P2 | 复用生图 + ai_models.reference_images 写入 | 资产板用途(写入资产 reference_images);商品用途已在 P1 行;3D 导演台的平替 |
| ★资产 图片工具条:九宫格 | 对标 | 做 | P2 | 复用生图链路 | 服务表情九宫格;资产板用途(写入资产 reference_images);商品用途已在 P1 行 |
| ★资产 图片工具条:宫格切分 | 对标 | 做 | P2 | 新建 S(切图) | §三明列,服务漫剧资产 |
| 图片工具条:人像质感调节/全景/打光/25宫格 | 对标 | 裁 | — | — | 总纲§三明裁长尾 |
| 图片工具条:局部重绘/标注 | 对标 | 裁 | — | — | 总纲§三明裁 |
| 图片工具条:扩图/擦除/抠图 | 对标 | 裁 | — | — | 默认裁判据(不在双旅程关键路径);见建议调整④ |
| 图片空态快捷:图生图/图片高清 | 对标 | 做 | P1 | 新建 S | 节点内空态,区别于画布级四快捷位 |
| 视频节点:五模式(文生视频/全能参考/图生视频/首尾帧/图片参考) | 对标 | 延 | P2 | 复用 /api/video-batch/models/submit 网关 | 枚举完整;按能力矩阵渲染(模型支持啥显啥,不照抄五 tab),按上游图灰置。**2026-08-09 用户裁决延 P2**(R2-Q4):`src/lib/video-models/types.ts` 的 `VideoGenerationMode` 只有 `image_to_video`/`prompt_to_video` 两值,七个模型 `supportedModes` 全声明这两个;`referenceRoles` 的 `first_frame`/`last_frame` 是零引用死字段——能力矩阵本身没有另三个模式 |
| 视频 chip:+参考 | 对标 | 做 | P1 | 复用 reference-cache.ts | |
| 视频 chip:标记 | 对标 | 裁 | — | — | 同图片标记,绑定裁 |
| 特效库(社区素材市场) | 对标 | 裁 | — | — | 明裁 |
| 特效作视频参数(预设 chips→提示词注入) | 对标 | 做 | P2 | 新建 S(与运镜 chips 同构) | 总纲 §五「特效/运镜进参数」 |
| ★资产 视频 chip:角色库 | 对标 | 做 | P2 | 复用 src/components/character-picker.tsx + 网关 DTO characterAsset 字段 | |
| 视频 chip:运镜(预设 chips:推镜/拉镜/摇镜/跟镜/俯拍/仰拍) | 对标 | 做 | P2 | 新建 S(chips→提示词注入) | §五「特效/运镜进参数」;与分镜表运镜列同源;运镜广场预设镜头词条(固定镜头/跟随拍摄/盘旋抬升/盘旋下降/上下左右摇)作 chips 扩充候选一并收编(纯提示词注入,成本≈0);仅裁示范视频卡+收藏+自定义+搜索的市场形态。五轮实弹:LibTV 预设注入=提示词框内 inline 结构化 chip(🎥镜头盘旋上升),与 @引用同构——我方注入形态照此 |
| 运镜库三 tab(运镜广场示范视频卡/我的收藏/我的运镜+搜索) | 对标 | 裁 | — | — | 广场/收藏/自定义属社区市场形态,同风格广场裁;保留形态=预设 chips;见建议调整⑤。五轮实弹:「我的运镜」=名称+运镜提示词(纯文本)+可选预览图——文本节点 @ 引用平替实证成立 |
| 视频参数:比例 7 种(Auto/16:9/4:3/1:1/3:4/9:16/21:9) | 对标 | 延 | P2 | 能力矩阵渲染 S | 枚举完整;实显以模型为准。**2026-08-09 用户裁决延 P2**(R2-Q4):`VideoAspectRatio = "9:16" \| "16:9"`,七个模型 `supportedAspectRatios` 全声明这两个——多渲染的档位无处落地(参照本表撤回过的图片画幅教训:多出的比例会静默回落 auto 却按全价扣分) |
| 视频参数:清晰度 480P/720P/1080P/4K | 对标 | 延 | P2 | 能力矩阵渲染 S | 枚举完整。**2026-08-09 用户裁决延 P2**(R2-Q4):该维度在我方能力矩阵中不存在——`VideoQuality = "standard" \| "hd"`,面板的「质量」是这一维,与 P 档不是一回事 |
| 视频参数:时长选择(按模型能力渲染离散档位) | 对标 | 做 | P1 | 复用网关时长参数 | **2026-08-09 用户裁决:只改措辞不改裁决**(R2-Q4)。原文为「时长滑杆」,但能力矩阵里多数模型只有 1-2 档离散时长(happyhorse=5/12s、sora2=12s),物理上做不成连续滑杆;现实现为按 `supportedDurations` 渲染的下拉,**改完即算已实现** |
| 视频参数:生成音频开关(原生音画同步) | 对标 | 做 | P2 | 网关 DTO 扩展+能力矩阵渲染 M | 需网关 DTO 加 generateAudio 透传+能力矩阵 supportsNativeAudio 字段+与 TTS 互斥规则(开原生音频的镜跳过 TTS);属参数扩展非 fork;上线前置=该模型含音频价已有明确结论(供应商不加价则 pricing.ts 明文注记 amount 不变),否则延至价目决策后 |
| 视频参数:生成数量 1/2 个 | 对标 | 延 | P2 | 复用批量提交 | 枚举完整。**2026-08-09 用户裁决延 P2**(R2-Q4):画布是一节点一 generation,不经批量网关;要支持 N 个须先有 P2 的批量记账(1 batch_id+N 子任务独立扣退),否则扣费与节点状态机对不上 |
| 视频模型清单(LibTV 15+ 对照) | 对标 | 做 | P1 | 复用 models/submit 7 模型网关(零 fork、允许参数扩展) | ADR2 铁律:零 fork、允许参数扩展 |
| 视频编辑:画布内裁剪/超分(高清) | 对标 | 裁 | — | — | 默认裁判据;见建议调整④;五轮实弹:LibTV 视频完成态工具条实测=裁剪/高清/解析/智能去字幕/音频分离/下载/全屏 |
| 视频编辑:插帧 30-60-90fps | 对标 | 裁 | — | — | 五轮实弹:实测工具条已不见插帧(可能下线);裁得更稳 |
| 视频编辑:智能去字幕/音频分离 | 对标(五轮实弹新录) | 裁 | — | — | 后处理工具类,非双旅程路径 |
| 视频节点:截帧(播放控制条相机钮,取当前帧→图) | 对标(五轮实弹新录) | 延 | P3 复盘 | 前端 canvas 截帧 S | 取帧作下游素材/首尾帧有真实价值;P3 复盘等量交换候选 |
| 视频编辑:拉片分析(解析) | 对标 | 延 | P4 | 复用 blueprint-drawer isDeconstruct 分支 + src/lib/studio/deconstruct.ts | =拆解节点,§三延列表;structure_only 纪律原样继承。五轮实弹:LibTV「解析」⚡2≈20s,产出=画布新挂「视频故事」表格节点(镜号/起止时间/时长/画面描述),**P4 拆解节点画布形态照此=生成下游结构化节点,非弹窗报告** |
| 视频空态快捷:首帧生成视频/首尾帧生成视频 | 对标 | 延 | P2 | 新建 S | **2026-08-09 用户裁决延 P2**(R2-Q4):前置是同组「视频节点:五模式」——首尾帧模式本身不存在,快捷位无处可去 |
| 音频独立节点(TTS) | 对标 | 裁 | — | — | 总纲明裁:无时间线消费方=死胡同;TTS 作脚本/视频节点参数 |
| TTS 停顿 `<#>`/语气词 `()` 文内标记 | 对标 | 延 | P2 评估 | 视 TTS 引擎能力 | 随 TTS 参数化落点(脚本节点配音)评估是否支持标记语法 |
| TTS 文本框 5 万字上限 | 对标 | 裁 | — | — | 随音频节点裁;分镜对白天然按镜分段 |
| 音色库选择(作脚本/视频节点参数) | 对标 | 做 | P2 | 复用 blueprints.globals.voice_id + ai_models.voice_id | LibTV 对应 max-speech-2.8-hd 音色库 |
| TTS 翻译图标 | 对标 | 裁 | — | — | 非旅程路径 |
| 音乐生成(Mureka V8) | 对标 | 裁 | — | — | BGM 用现有 globals.bgm_style 参数,不接音乐生成模型 |
| 音频空态快捷:音频生视频 | 对标 | 裁 | — | — | 随节点裁;画布空态位④「文案→配音视频」替代该起点 |
| 脚本节点=blueprints 画布宿主,双击打开蓝图抽屉 | 独有 | 做 | P2 | 组件级复用 src/components/studio-shell/blueprint-drawer.tsx,不 fork 不重写 | §六第一条 |
| 三步向导(①确认镜头②准备资产③合成提示词)+顶栏进度「1/3,完成后可批量生视频」 | 对标 | 做 | P2 | blueprint-drawer 增量 M | LibTV 全屏三步;我方以抽屉为宿主;四轮细节:步骤条带计数(17镜就绪/0/7已生成还差7个/0/17已合成);对白空值『+』点击添加;最终提示词列『待生成提示词』占位 |
| 分镜表 9 维度列(镜号/时长/画面描述/景别/光影氛围/对白·旁白/音效/运镜/最终提示词+操作列) | 对标 | 做 | P2 | blueprints.scenes jsonb 扩展(现有 idx/line/visual/slot/duration_ms/beat 上加列)M | §六唯一增量① |
| 分镜行拖拽排序(左侧拖拽手柄) | 对标 | 做 | P2 | 新建 S(jsonb 数组重排) | 四轮实测 |
| 按轨默认列显隐(电商轨默认 4 列:镜号/画面描述/台词/时长,其余折叠「高级列」;漫剧轨 9 列全显) | 复审 | 做 | P2 | 新建 S | 引擎照样全填 |
| ★资产 画面描述 @资产 引用+蓝色高亮 | 对标 | 做 | P2 | 新建 M(@提及编辑器) | @角色/@场景/@道具,注入编译 |
| 景别枚举 10 种 chips | 对标 | 做 | P2 | 新建 S | 枚举完整=大远景/远景/全景/全身景/中景/半身像/中近景/近景/特写/大特写 |
| 运镜列=自由文本+预设 chips | 对标 | 做 | P2 | 新建 S(与视频 chip 同源) | |
| 逐镜独立 LLM 生成(光影/音效/运镜逐镜非模板;有台词的镜写对白) | 对标 | 做 | P2 | 复用导演引擎链路 | |
| 添加镜头 | 对标 | 做 | P2 | 复用 blueprint-drawer 场编辑 | |
| ★资产 ③合成提示词编译(分镜信息+引用资产→逐镜最终提示词) | 对标 | 做 | P2 | 新建 M(编译器) | 资产描述注入是漫剧一致性落点 |
| 脚本节点顶栏动作:重新生成/批量生分镜图/批量生视频/导出 | 对标 | 做 | P2 | 批量走 BTM+现网关,新建 M | §六唯一增量③ |
| 导演引擎两段式(deepseek 撰稿+segmenter 气口分段+qwen hooks 四型;机械稿即时零白屏→导演稿异步 diff 覆写,每镜可还原) | 独有 | 做 | P2 | 复用 v1 导演层引擎设计+现有 hooks 链路 | 导演稿免费+滥用闸见 F 组 |
| 继承 blueprint-drawer 现能力(台词行内编辑/hook 矩阵/配方实例化/卖点勾选) | 独有 | 做 | P2 | 复用 blueprint-drawer.tsx(2026-07-05 已过人工走查) | 零重写 |
| 批次进度面板(总进度+逐镜四态格+积分实扣/实退实时数) | 独有 | 做 | P2 | 新建 M | §六批量体验 |
| 失败镜一键单独重试(成功镜不重扣) | 独有 | 做 | P2 | 依赖 F 组批量记账 | P2 验收模板项 |
| 先试 N 镜(批量生视频默认勾选前 3 镜试产,满意再全量) | 复审 | 做 | P2 | 新建 S | 漫剧 ⚡2300/片 的留存保护 |
| 批量失败分型(可重试类显重试钮;内容审核拒类显「改画面描述」直达该镜编辑) | 复审 | 做 | P2 | 新建 S | 漫剧打斗/武器题材高频触发审核拒 |
| 批量生视频门控=缺图镜跳过+警示哪些镜降质 | 复审 | 做 | P2 | 新建 S | 门控软化同源 |
| 节点角标 n/m 常驻 | 独有 | 做 | P2 | 新建 S | |
| 合成节点:多视频拼接成片 | 对标 | 做 | P3 | 复用 ai-gen/stitch 双通道 + src/lib/studio/local-stitch.ts | 已裁决方案 a(P1 旅程 A 以单视频下载/直发收口,合成节点 P3),见建议调整②;LibTV 实测:合成节点纯下游、空态无参数面板「请连接视频节点后操作」 |
| 合成节点:音频连线输入 | 对标 | 裁 | — | — | 音频节点已裁;配音经 TTS 参数在镜内合入 |
| 合成 DB 排队+节点显示排队位 | 独有 | 做 | P3 | 新建 M(队列表) | ADR7,禁 fail-fast 甩用户 |
| 合成轮播模式(承接幻灯片能力) | 独有 | 延 | P4 评估 | — | §五「后期评估」 |
| 成片时间线 | 复审 | 延 | P4 评估 | — | 必须以合成节点数据结构为底座演进 |
| 节点白名单纪律(6 类 v1;新增节点须对标复核+用户裁决) | 独有 | 做 | P0 | 纪律,零代码 | §三 |

## C. 资产系统

| 功能点 | 来源 | 裁决 | 期次 | 复用或新建 | 备注 |
|---|---|---|---|---|---|
| ★资产 三类资产统一模型(角色/场景/道具,每类可+新增) | 对标 | 做 | P2 | 角色复用 ai_models 表(020+20260313 迁移);场景/道具落表方案待双轨数据模型设计 | 三库同源红线:优先扩展现表(character_type 已有),不建平行表 |
| ★资产 AI 自动从分镜识别资产并归类挂「待创建槽」 | 对标 | 做 | P2 | 复用 blueprints.scenes[].slot{kind,asset_ref} + LLM 识别新建 M | LibTV 实测:17 镜识出 4 角色+2 场景+1 道具 |
| ★资产 资产 AI 详细设定描述自动生成(角色=年龄/身高/体型/发型/脸型/瞳色/肤色/标志特征/服装/神态+生成规则) | 对标 | 做 | P2 | 新建 M(LLM 生成) | |
| ★资产 一致性黄金 prompt 模板化(白底/棚拍平光/4 视图布局/面发体服一致约束/禁道具文字水印/[视觉风格:X] 占位) | 对标 | 做 | P2 | 新建 S(prompt 模板,直接复用 LibTV 实测原文) | 双轨一致性底座核心 |
| ★资产 设定图 4 视图(正面半身特写+全身正面居中+左侧面+背面) | 对标 | 做 | P2 | 复用 /api/generate/image 生成 | |
| ★资产 表情九宫格 | 对标 | 做 | P2 | 复用生图+宫格切分 | 角色资产卡组成件 |
| ★资产 多角度呈现板 | 对标 | 做 | P2 | 复用 ai_models.reference_images TEXT[](20260313 迁移) | 亦是 3D 导演台平替 |
| ★资产 场景资产卡(室内/外、尺度、背景描述) | 对标 | 做 | P2 | 新建 S | |
| ★资产 道具资产卡(六视图详细描述) | 对标 | 做 | P2 | 新建 S | 四轮已实录 LibTV 道具黄金 prompt 全文结构(外观六视图→材质细节→关键细节→格子居中无畸变→负面清单→材质结构一致→[视觉风格:X]),直接作我方 prop_6view 模板底稿,见 INVENTORY |
| ★资产 全局风格标签(整片统一风格) | 对标 | 做 | P2 | 复用 blueprints.globals 扩展字段 S | 如「盛唐古风·国风二次元」 |
| ★资产 同一资产多版本(如现代/唐朝同一角色) | 对标 | 做 | P2 | canvas_assets 兄弟行(共享 character_model_id)+variant_label,建表自带零额外迁移;ai_models 零改动 | 生成第二版本时用第一版本 reference_images 垫图保脸;P3 再评估跨表 variant_group |
| ★资产 资产三来源:AI 生成/从当前画布选择/本地上传 | 对标 | 做 | P2 | 画布选=复用节点引用;上传=复用 OSS;AI=复用生图 | |
| ★资产 一键生成所有资产(批量出全部设定图) | 对标 | 做 | P2 | 走 BTM 批量+F 组批量记账 | §六默认按钮 |
| ★资产 资产门控软化(不齐默认自动生成/可跳过,仅警示「哪些镜会降质」;硬门控仅零输入) | 独有 | 做 | P2 | 新建 S | LibTV 硬门控(0/7 未生成步③灰)的电商友好改造;LibTV 形态=底部警示条『检测到 N 角色 M 场景 K 道具没有设定图,可手动上传或 AI 批量生成』+步③点击无响应(硬门控);我方软化但警示条样式可参照 |
| ★资产 电商资产映射(商品→道具、模特→角色、拍摄场景→场景) | 独有 | 做 | P2 | 复用同一资产管线 | 双轨统一结论落点:下游全共用 |
| ★资产 角色卡字段复用(dna_config/voice_id/character_type/source/owner_id/preview_video_url) | 独有 | 做 | P2 | 复用 020_character_system.sql+20260313+20260314 迁移、src/types/character.ts | 零新表 |
| ★资产 资产写入 canvases.deps 依赖清单 | 独有 | 做 | P2 | 复用 P0 已备 deps 字段 | 服务 P3 分享复制校验 |
| 资产图统一存 object key+generations 为文件唯一真相源+删除只标不删 | 复审 | 做 | P2 | 沿用 ADR5 机制 S | 防签名 URL 过期资产卡集体挂 |
| 商品双身份规则(卖点文本→上游输入;商品图→自动建道具类资产,@商品即引用) | 复审 | 做 | P2 | 新建 S | 双轨切换语义定死 |
| 3D 导演台(Three.js 摆姿人偶 20 姿势+逐关节滑杆+机位取景) | 对标 | 裁 | — | — | 总纲明裁,用多角度图生成平替 |
| 角色三视图空态快捷位(LibTV 起点②) | 对标 | 裁 | — | — | 由资产装配内「AI 生成」路径覆盖,不占画布空态位 |

## D. 生成器面板

| 功能点 | 来源 | 裁决 | 期次 | 复用或新建 | 备注 |
|---|---|---|---|---|---|
| 生成器面板挂节点下方 | 对标 | 做 | P1 | 新建 M(面板壳) | §三 |
| 模型选择器(按节点类型列模型) | 对标 | 做 | P1 | 复用 src/lib/studio/job-spec.ts + recipes.ts 模型枚举 | |
| 能力矩阵驱动参数渲染(模型支持啥显啥) | 独有(复审) | 做 | P1 | 新建 M(能力矩阵表) | 五模式/比例/清晰度/音频开关统一由此驱动 |
| 参数胶囊 | 对标 | 做 | P1 | 新建 S | |
| 胶囊≥5 折叠「更多」 | 独有 | 做 | P1 | 新建 S | 1366×768 条款 |
| @引用素材(@节点/@历史/@资产) | 对标 | 做 | P1(@资产 P2) | 新建 M(提及选择器) | LibTV 另一表达=提示词内『图N』指代上游(基于图1生成…与图1保持一致),我方 @ 引用编译时等价 |
| 积分预估显示(estimateCredits 与结算 import 同一函数) | 独有 | 做 | P1 | 新建 src/lib/credits/pricing.ts | 详见 F 组件套① |
| 费用预估显示(总额+余额,随参数实时更新) | 独有 | 做 | P1 | 新建 S | §七。**2026-08-09 用户裁决:「恒显费用条不需要」** —— 原文为「费用汇总条(明细+总额+余额,发送钮上方恒显,替代弹窗)」,现改为本行措辞,**改完即算已实现**(面板发送钮左侧常显「预计 N 积分 · 余额 M」,随参数实时变;无报价时按钮不可点,不存在「能点但金额空白」的窗口)。取消的两项各有理由:**明细**——定价模块只算单一 cost,无明细数据源;**发送钮上方恒显独立条**——用户明确不要。原文那句「替代弹窗」的意图由 #185 双阈值 + 出处闸承担,而「提交后不可取消、不退款」这句事前告知改挂生成按钮 `title`(按 `supportsCancel` 判空,将来有可撤单模型会自动退场) |
| 拦截式确认仅限「余额<预估×1.2 或单次>**1000**⚡」 | 独有 | 做 | P1 | 新建 S | P1 验收「超阈值确认」。**2026-08-09 用户裁决①:按原规格改代码**(R2-Q4)——此前实现为 `needsConfirmation: cost > 0`(每次付费都弹),方向与本行相反;已改为双阈值,常量与纯函数在 `src/lib/canvas/generation-pricing.ts`。**裁决②(同日,审计后):阈值 5000 → 1000** —— 原值在本产品**物理不可达**(画布单次天花板=happyhorse 12 秒 1080),那条臂是死支路,「双阈值」实际塌缩成单条低余额规则;1000 亦与主站 omnibox 的 `CONFIRM_CREDITS_THRESHOLD` 对齐(此前画布反而比主站宽 5 倍,而画布是不可退款的那条链路)。backend verifier 有守卫穷举全目录价目、断言阈值低于天花板,防它再变回死支路。另加正交的**出处闸** `generation-consent.ts`,它**永不削弱本行的金额判定** |
| 灰置控件点击可得「如何解锁」指引(如「需要一张图片,[创建图片节点]/[从历史选择]」) | 独有 | 做 | P1 | 新建 S | §五 |
| 所有模式/参数名一句话人话+悬停示例 | 独有 | 做 | P1 | 新建 S(文案) | §五 |
| 发送(Ctrl+Enter)+防重复提交(狂点只一个 job 一次扣) | 对标+独有 | 做 | P1 | 新建 S(提交幂等 key) | P1 验收模板项;快捷键注册随 A 组快捷键体系 |
| 面板超视口自动 dock 底部 | 独有 | 做 | P1 | 新建 S | 1366×768 条款 |

## E. 平台层

| 功能点 | 来源 | 裁决 | 期次 | 复用或新建 | 备注 |
|---|---|---|---|---|---|
| 角色库入口(底部栏,一键「应用至画布」) | 对标 | 做 | P2 | 复用 ai_models 表 + src/components/character-picker.tsx | 三库同源直读 |
| ★资产 预置演员卡内容(甜妹/霸总/古风男女主/正反派长辈等;每卡=全身+面部特写+表情九宫格+多角度板) | 对标 | 做 | P2 | 新建 M(内容生产,走自家资产管线) | 结构复用角色资产卡;四轮:卡带标签体系(女主/女/现代/青年/温柔)+角色筛选下拉+最近使用过滤;卡=全身+面部特写+表情九宫格×2+全身三视图+头部多角度九宫格 |
| 素材库·风格库(社区风格市场:商用标识/作者/使用量/推荐/MJ/摄影写真/电商营销/动漫游戏等 11 分类) | 对标 | 裁 | — | — | 风格广场社区市场明裁 |
| 素材库·特效库 | 对标 | 裁 | — | — | 明裁 |
| 历史资产库(跨画布图/视频/音频生成历史,从中选素材建节点) | 对标 | 做 | P0 | 直读 generations 表(library_status/batch_id/spec)+assets/blueprints,不建平行表 | ADR8 三库同源;四轮:LibTV 形态=图/视频/音频三 tab 带计数+按日期分组+时间排序切换+批量操作+面板内缩放 |
| 历史资产批量操作 | 对标 | 延 | 按需 | — | 总纲未列;P1 后按等量交换原则复议 |
| 我的工作流(成组存结构模板) | 对标 | 做 | P3 | 新建 M(组→模板序列化) | §九 P3;LibTV 实测范式:模板=成组预连线节点+每节点指引性标题+组内「成片视效参考」示例节点;四轮实测:LibTV 入口=组工具条『添加到工具箱』;插入画布=成组预连线+指引性标题+成片参考示例节点 |
| 官方预设工作流模板(电商手机弹出/360旋转展示/咖啡杆出场/机械臂视角/Live2D) | 对标 | 延 | P4 | — | 随风格模板市场触发条件(社区发布起量)一并评估 |
| 风格模板市场 | 对标 | 延 | P4 | — | 触发=社区发布起量 |
| 实时多人协作(Yjs) | 复审 | 延 | P4 | — | 触发=团队客户明确要 |
| 工作流 vs 配方分工声明(工作流=结构模板;配方=内容参数模板留 /templates,互不越界) | 独有 | 做 | P3 | 复用 src/lib/studio/recipes.ts + blueprints.recipe_id | §九 P3 明文 |
| 分享:发布到社区 | 对标 | 做 | P4 | — | 总纲 §九 P4 计划内容,非延项 |
| 分享链接 v1(免登录只读+复制到我的空间+deps 依赖清单校验) | 对标+独有 | 做 | P3 | 复用 canvases.deps 字段,新建 M | P3 验收模板项 |
| 复制遇依赖缺失(模型下线/音色无权限)逐项红字+可替换,不复制出坏画布 | 独有 | 做 | P3 | 新建 S | P3 验收模板项 |
| 直发 TikTok 产物动作 | 独有 | 做 | P1 | 复用现有直发/发布链路 | 旅程 A 出口;电商轨独有 |
| 直发 TikTok 自动 AIGC 标注 | 独有(复审) | 做 | P1 | 复用 src/lib/tiktok/content-posting.ts 既有 aigc 字段 S | 合规默认项,非用户开关;国内分发合规 P3 评估 |
| 交互式教程双版本(电商版 P1/漫剧版 P2,跟手走完最短链)+完成奖励小额积分 | 独有 | 做 | P1/P2 | 新建 M | 奖励=积分发放链路首个真实用例(F 组) |
| CLI & Skill Agent 接口(UUID+Access key) | 对标 | 裁 | — | — | Agent 会话 API 明裁 |
| AI 水印设置 | 对标 | 裁 | — | — | 非旅程路径;AIGC 标注已拆出为合规默认项 P1 做(见本组直发 AIGC 标注行),见建议调整③ |
| 暗/亮模式切换 | 对标 | 裁 | — | — | 沿用站点既有主题体系,画布不单做 |
| 存储 60G 配额 | 对标 | 裁 | — | — | 会员数值,框架期零变更 |
| 订阅/开发票/会员档 VIP/活动权益(限时折) | 对标 | 裁 | — | — | 会员营销明裁;沿用站点现有计费页 |
| QQ/微信登录 | 对标 | 裁 | — | — | 总纲明裁 |
| 会员营销弹窗 | 对标 | 裁 | — | — | 总纲明裁 |

## F. 积分

| 功能点 | 来源 | 裁决 | 期次 | 复用或新建 | 备注 |
|---|---|---|---|---|---|
| billing_mode 双模式声明(每 action 显式 post_charge/hold_settle/free) | 独有 | 做 | P1 | 新建 S(枚举字段) | hold_settle P3 才启用 |
| 件套①estimate API:pricing.ts 单一常量模块+PRICING_VERSION 回传+服务端重算>预估拒单;同 PR 收敛 4 处散落定价 | 独有 | 做 | P1 | 新建 src/lib/credits/pricing.ts;改造 deduct 路由 CREDIT_PRICING/types VIDEO_MODEL_PRICING/src/lib/credits.ts(@deprecated)/actions.generate-video | 预估=实扣的机制保证 |
| 件套②下线无鉴权路由 /api/user/credits/{deduct,refund} | 独有 | 做 | P1 | 删改 src/app/api/user/credits/{deduct,refund}/route.ts | 安全洞:userId 取请求体+admin client 直改余额+deduct 不写流水;P1 前必须处理 |
| 件套③退款幂等下沉 DB:credit_transactions 部分唯一索引 unique(type,(metadata->>'task_id')) where type='refund' + on conflict do nothing | 独有 | 做 | P1 | 新建迁移 S | 应用层 check 仅优化 |
| 件套④流水标准化:type 枚举定死(consume/refund/free_usage/grant)+task_id/canvas_id/node_id/batch_id/action_id 结构化列+索引;删 insertCreditTransaction 试插循环;用户明细页=按 canvas/batch 聚合只读 | 独有 | 做 | P1 | 新建迁移 M | |
| 件套⑤客户端零金额(前端只传 action+params+estimateToken,金额扣费全在服务端提交事务) | 独有 | 做 | P1 | 改造提交链路 M | |
| 框架期零价目变更铁律(图 1K/2K/4K=5/10/15、视频各模型现价、assembly 1⚡/镜先渲后扣、hook/导演稿免费、拼接=assembly 现价不另立 0 价) | 独有 | 做 | P1 | 纪律+pricing.ts 常量 | 新节点接入沿用宿主链路现价 |
| free 模式配额闸(导演稿每用户 50 次/日+单蓝图免费重生成 5 次,超按脚本价;服务端 429 fail-fast;入 amount=0 流水) | 独有 | 做 | P2 | 新建 S | |
| 批量记账(1 batch_id+N 子任务独立 task_id 独立扣/退;禁 lump deduct;结果条「成功 x/N,已退 y⚡」;流水按镜可查) | 独有 | 做 | P2 | 复用 generations.batch_id,新建 M | P2 前置;直扣模式天然支持部分退 |
| 并发扣费 Postgres RPC 单语句原子(UPDATE...WHERE credits>=amount),CAS 循环仅留低频路径 | 独有 | 做 | P2 | 新建 RPC 迁移 S | 随批量记账落地 |
| hold_settle 表结构落库冻结不启用(credit_actions/credit_holds) | 独有 | 做 | P1 | 新建迁移 S(DDL 草案见 planreview-credits) | 防后续迁移 |
| hold_settle 启用(悬挂≤10 分钟自动释放+对账脚本可查悬挂单+settled≤estimated 库级 check+超收平台吸收+在途按 pricing_version 锚定) | 独有 | 延 | P3 | 复用 P1 冻结表结构 | 触发条件=大额批量场景+悬挂释放对账脚本齐备 |
| 预估=实扣逐条比对+当日积分对账差异=0 验收 | 独有 | 做 | P1 | 新建验收脚本 S | 每期通用验收 |
| 积分发放(grant)首个真实用例=教程完成奖励 | 独有 | 延 | P2 | 复用件套④ grant 类型 | **2026-08-09 用户裁决延 P2**(R2-Q4):前置的 E 组「交互式教程双版本」入口硬关(`src/components/canvas/canvas-chrome-policy.ts:49` 为 `{ id:"tutorial", enabled:false }`),教程本体不存在,首用例无处触发。与教程同期落地;grant 类型与件套④ 流水列**保持不动**,届时直接接 |
| 双积分池(通用+平台积分+消耗顺序设置) | 对标 | 裁 | — | — | 现单池;框架期零变更,仅作后续定价讨论输入 |
| LibTV 定价参照(2K 图⚡18/Seedance2.0 5s720P⚡135/脚本⚡6/Seedance 5 积分/秒/年费 499/1299/2999/6599/¥199=3000 分/¥499=7500 张) | 对标 | 裁 | — | — | 明文「仅作后续定价讨论输入」,不落价目 |
| 积分 6 个月有效期/支付后不退 | 对标 | 裁 | — | — | 同上 |

## G. 稳定性与性能

| 功能点 | 来源 | 裁决 | 期次 | 复用或新建 | 备注 |
|---|---|---|---|---|---|
| 组件级错误边界+「尝试恢复/重新加载画布」按钮 | 对标 | 做 | P0 | 新建 S | LibTV 实测崩过 React #185,照抄其恢复 UI |
| store 层防护+错误边界触发进监控(单画布>1 次/日报警) | 独有 | 做 | P0 | 新建 S | |
| zod 加载校验+非法节点降级「损坏节点」占位卡(可删),永不整画布白屏 | 独有 | 做 | P0 | 新建 M(zod schema) | P0 验收:塞坏 node json 画布照开 |
| schema 迁移注册表(v1→v2→…;上一版本文档能打开进 DoD) | 独有 | 做 | P0 | 新建 S | |
| 内容节点删除二次确认 | 对标 | 做 | P0 | 新建 S | §三明列 |
| 删除 running 节点三选一(取消并退款/仅移除[任务继续产物进历史]/返回);网关不支持取消则明示 | 独有 | 做 | P1 | 新建 S | §七 |
| 节点状态机(idle→queued→running→done/failed;failed 单独重试;刷新经对账恢复) | 独有 | 做 | P1 | 复用 generations 状态 + BTM 乐观 UI(src/components/background-task-manager.tsx,浏览器内编排器不 fork) | generations=唯一真相源,画布只存 nodeId→taskId 引用 |
| 对账恢复验收(提交后 kill 标签→重开→收敛服务端真值;**未 bound 行**超时判死触发幂等退款) | 独有 | 做 | P1 | 依赖 A 组对账合约 | P1 验收模板项。**2026-08-09 用户裁决:已 bound 到厂商 task 的行不做自动判死退款,该半延 P2**(R2-Q4/#51①)——厂商侧慢任务与真失败在我方不可区分,自动退款有误退真实成功任务的风险(退了钱又出了片),且 `fail_canvas_generation_v1` 已明文 RAISE 禁止对 unknown 自动退款,绕过它等于绕过该设计。P1 内改为**运维告警**:bound 行超 `maxPollMs` 仍未终态即报警转人工裁决。现覆盖面=三个 sweep(`sweep_stale_canvas_not_started_v1` / `sweep_stale_canvas_text_submission_unknown_v1` / `sweep_expired_canvas_uploads_v1`),全部只管未 bound 行 |
| 双标签无双跑双扣 | 独有 | 做 | P1 | 依赖单写者锁+提交幂等 | P1 验收模板项 |
| 媒体降级策略(节点默认 poster 缩略图/选中才挂 `<video>` 且 DOM 断言进 DoD/同屏活跃视频≤6/低 zoom 语义缩放降级色块) | 独有 | 做 | P0 | 新建 M | ADR1 |
| 性能预算数值验收(200 节点 pan/zoom≥50fps 中端机/冷加载<3s/100 节点+30 视频 poster 内存<800MB/100 节点保存<1s) | 独有 | 做 | P0 | 新建验收脚本 S | P0 DoD |
| 文档 >2MB 硬拒存并提示 | 独有 | 做 | P0 | 新建 S | P0-Q1 已裁(2026-07-12):2MB=硬拒存(D3 返 400 + S7 toast);软告警在 >512KB(见 A 组) |
| 批量/合成重任务一律 DB 排队+排队位显示,禁 fail-fast(local-stitch 并发闸=2,3.4G 小机) | 独有 | 做 | P2 批量/P3 合成 | 复用 src/lib/studio/local-stitch.ts + 新建队列 M | ADR7 |
| 批量生分镜图先落库排队(batch 预创建 generations queued 行+并发闸 2-3+429 退避;关标签经对账恢复) | 复审 | 做 | P2 | 复用 generations.batch_id+spec,新建 M | 图片腿无批量网关,ADR7 在图片腿的承载物 |
| 注入上游失败→节点 failed+恰好一条 refund | 独有 | 做 | P1 | 依赖 F 组幂等退款 | P1 验收模板项 |
| 每期验收脚本惯例 .temp/canvas-p{n}-verify.mjs + tsc/build/实弹/大改动对抗审查 workflow | 独有 | 做 | P0 | 新建各期脚本 S | 工程纪律 §十;P0 首落、每期沿用(期次填裸「P0」便于机器一一对应,勿写「P0 起」等变体) |
| 真人双旅程走查(停留>60s 无操作=卡点;卡点>2 不发布;旅程 A≤7 步/B≤9 步) | 独有 | 做 | P1 起 | 流程,零代码 | §二 |
| 收编量化判据(完成率≥/studio 同链路+P95 出片不劣化+连续 14 天零 P0/P1+对账抽样 100 单零差异;全绿再观察 14 天才折叠旧入口) | 独有 | 做 | P3-P4 | 新建监控查询 S | ADR9 |

---

## 建议调整(②③已裁决;④⑤仍供后续复盘裁决;原①⑥已裁决并删除)

2. **合成节点期次矛盾**:已裁决:方案 a——P1 旅程 A 以单视频下载/直发收口,合成节点 P3;总纲 §二已同步改。
3. **AI 水印/AIGC 标识**:已裁决:水印设置面板不做,AIGC 标注 P1 做(见 E 组「直发 TikTok 自动 AIGC 标注」行,合规默认项非用户开关)。
4. **图片扩图/擦除/抠图、视频超分/插帧、历史批量操作**:总纲未明列,按「不在双旅程关键路径默认裁/延」处理;电商用户对抠图(商品去底)诉求概率高,建议 P3 复盘时作等量交换候选。
5. **运镜库三 tab**:广场/收藏/自定义形态按社区市场同类裁,仅保留预设 chips 进参数;若漫剧用户对自定义运镜有真实诉求,P4 与风格模板市场同一触发条件评估。

## 统计

**各组裁决计数**

| 组 | 做 | 裁 | 延 | 合计 |
|---|---|---|---|---|
| A. 画布壳 | 41 | 0 | 2 | 43 |
| B. 节点 | 50 | 17 | 17 | 84 |
| C. 资产系统 | 19 | 2 | 0 | 21 |
| D. 生成器面板 | 13 | 0 | 0 | 13 |
| E. 平台层 | 11 | 9 | 4 | 24 |
| F. 积分 | 12 | 3 | 2 | 17 |
| G. 稳定性与性能 | 18 | 0 | 0 | 18 |
| **合计** | **164** | **31** | **25** | **220** |

**「做」按期次分布**(跨期项计入起始期;E 组教程计 P1;G 组排队计 P2;G 组收编判据计 P3;C 组全落 P2)

| 期次 | 功能点数 | 主体 |
|---|---|---|
| P0 | 48 | 画布壳 35 + 节点空壳/文本/白名单纪律 3 + 历史库 1 + 稳定性 9 |
| P1 | 49 | 生成接入 16 + 生成器 12 + 积分件套 9 + 稳定性 6 + 平台(直发/AIGC 标注/教程)3 + 对账合约 1 + 五轮新录(输入已更新角标/上游自动入引用)2 |
| P2 | 55 | 脚本节点与批量体验 29 + 资产系统 19 + 积分 3 + 角色库/演员卡 2 + 批量排队 2 |
| P3 | 11 | 链接解析/合成+排队 3 + 分享/工作流 4 + 组工具条(整组执行/批量下载/避让顶栏)3 + 收编判据 1 |
| P4 | 1 | 发布社区 |

> P4 做=发布社区+收编判据收尾(收编判据跨 P3-P4,期次表计 P3);延项 24,另按需 1(历史批量),共 25——以本表实际计数为准。明细:
>
> - **原有 12**:成片时间线/实时多人协作 Yjs/风格模板市场/官方预设工作流模板/hold_settle 启用/拉片分析(拆解节点)/合成轮播/TTS 标记(P2 评估)/合并分镜组(P3 复议)/转分镜组(P3 复议)/图片 chip 聚焦(P2 评估)/截帧(P3 复盘,五轮新录)
> - **2026-08-09 用户裁决新增 9 项(全部延 P2,R2-Q4 混合路线)**:图片画质低/标准/高、图片生成数量 1/2/4、图片模型清单、视频五模式、视频比例 7 种、视频清晰度 P 档、视频生成数量 1/2、视频空态快捷首帧/首尾帧、grant 首个真实用例(教程完成奖励)。**共同判据=外部条件不具备(上游能力矩阵/请求体/在售模型里根本没有该维度),不是工时问题**;逐项证据见各行备注。裁决过程与反驳记录见 P0 看板 R2-Q4。
> - **另有一项半改判(不单独计数)**:G 组「对账恢复验收」的**已 bound 行超时判死→自动退款**这半延 P2,行本身仍为做/P1(改为运维告警口径),见该行备注。

**★资产 标记行数**:24(C 组 17 + B 组 6[工具条多角度/九宫格/宫格切分+视频 chip 角色库+@资产引用+合成提示词编译] + E 组预置演员卡 1)——即并入总纲§六「资产装配系统」时需新增的面。
